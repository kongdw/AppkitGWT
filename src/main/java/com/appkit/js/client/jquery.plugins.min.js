function focusable(a, c) {
    var b, d;
    b = a.nodeName.toLowerCase();
    if ("area" === b) {
        b = a.parentNode;
        d = b.name;
        if (!a.href || !d || "map" !== b.nodeName.toLowerCase())return !1;
        b = $("img[usemap='#" + d + "']")[0];
        return !!b && visible(b)
    }
    return (/^(input|select|textarea|button|object)$/.test(b) ? !a.disabled : "a" === b ? a.href || c : c) && visible(a)
}
function visible(a) {
    return $.expr.filters.visible(a) && !$(a).parents().addBack().filter(function () {
            return "hidden" === $.css(this, "visibility")
        }).length
}
$.extend($.expr[":"], {
    focusable: function (a) {
        return focusable(a, null != $.attr(a, "tabindex"))
    }, tabbable: function (a) {
        var c = $.attr(a, "tabindex"), b = null != c;
        return (!b || 0 <= c) && focusable(a, b)
    }
});
